# Python virtual environment
venv/
.venv/

# Python compiled files
__pycache__/
*.pyc

# Database file (if it's local development database)
spinalysis.db

# Node.js modules
node_modules/
npm-debug.log*
yarn-debug.log*

# Electron build artifacts (even if not building, avoid potential clutter)
dist/
build/
electron-builder.env

# PyInstaller build artifacts (even if not building)
*.spec

# OS generated files
.DS_Store
Thumbs.db

# Electron custom cache directory (from main.js)
cache/

# Git LFS tracking - VERY IMPORTANT TO INCLUDE THIS FOR electron.exe
frontend/node_modules/electron/dist/*.exe filter=lfs diff=lfs merge=lfs -text
backend/dist/*.exe filter=lfs diff=lfs merge=lfs -text